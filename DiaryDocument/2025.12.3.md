# 2025.12.6 开发目标（LangChain + FastAPI 聊天伙伴）
- 完成 FastAPI 最小可运行版本，注册基础路由与健康检查。
- 在 `config.py` 实现 GPU/模型路径检测与配置结构，支持环境变量覆盖。
- 起草 `chains/conversation.py` 与 `chains/rag.py` 的伪实现，跑通单轮对话 + 简单检索。
- 添加 pytest + httpx 的 API smoke 测试骨架，准备后续自动化。
