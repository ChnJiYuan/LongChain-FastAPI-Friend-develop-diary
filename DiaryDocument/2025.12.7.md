# 2025.12.7 Development Log (LangChain + FastAPI Chat Companion)
下一步工作（按优先级）：

完善 Memori SDK 集成：替换 backend/app/services/memory/memori_client.py 内的占位实现，接入真实 SDK，并在 .env.example 补充所需配置说明。
数据库/持久化梳理：补齐 ORM 模型与迁移（如果需要），确认 memori.db 的使用场景，避免生产写入 SQLite；为 Milvus 启动脚本增加健康检查与自动建表校验。
测试与验证：运行 pytest backend/app/tests，补充针对 Memori/Milvus 客户端的集成测试或 mock 测试；为前端添加基础 API 绑定的契约测试（可用 MSW 或简单 fetch mock）。
前端对接：在 frontend 接入后端 /api/v1/chat、/api/v1/memory 路由，展示检索到的 Memori/Milvus 证据块；准备基本 UI（会话、上下文侧栏、设置页）。
部署与配置：编写 .env 示例中的部署注释，增加生产模式 Docker Compose（如启用 Nginx 反向代理、TLS 占位），并在 README 添加部署步骤。
观测与日志：在 backend/app/core/logging.py 增加结构化日志示例（trace id、user id），并为关键链路（存取 Memori/Milvus、LLM 调用）添加超时和错误兜底。
文档与演示：在 docs/architecture.md 或 docs/erd.puml 中补充最新的数据流/ERD；提供一份 end-to-end 演示脚本（含 curl / 前端操作）。